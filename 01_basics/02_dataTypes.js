"use strict"; // treat all JS code as newer version

// alert(4+4); // We r using node.js not browser

let name = "Aditya";
let age = 20;
let isLoggedIn = true;
let state = null;

// Number => age,ranges between 2^53
// String => name, text,""
// Boolean => true,false
// null => standalone value ::typeof - object
// undefined => not given value
// symbol => unique

// object

console.log(typeof null); //